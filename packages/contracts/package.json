{
  "name": "@prism/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Protocol Buffers and cross-language generators for prism-core",
  "scripts": {
    "build": "npx buf generate",
    "generate": "npx buf generate",
    "clean": "rm -rf gen/"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.34.0",
    "@bufbuild/protobuf": "^1.10.0",
    "@bufbuild/protoc-gen-es": "^1.10.0"
  }
}